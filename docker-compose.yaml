version: '3.8'

services:
  go-discord-music:
    build: .
    container_name: go-discord-music
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
